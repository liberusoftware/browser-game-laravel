<section aria-labelledby="browser-game-moderation-and-analytics-heading"><h2 id="browser-game-moderation-and-analytics-heading">Moderation and Analytics</h2>@if($message)<p role="status">{{ $message }}</p>@endif @forelse($moderationAndAnalytics as $record)<article><h3>{{ $record->name }}</h3><p>{{ $record->kind }} · {{ $record->status }}</p>@if(in_array($record->kind, ['report','appeal','fraud','health'], true) && $record->status === 'open')<button type="button" wire:click="resolve('{{ $record->id }}')">Resolve</button>@endif</article>@empty<p role="status">No moderation and analytics records are available.</p>@endforelse</section>

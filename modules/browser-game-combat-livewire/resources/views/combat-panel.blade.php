<section aria-labelledby="browser-game-combat-heading"><h2 id="browser-game-combat-heading">Combat</h2>@if($message)<p role="status">{{ $message }}</p>@endif @forelse ($battles as $battle)<article><h3>{{ $battle->actor_id }} vs {{ $battle->opponent_id }}</h3><p>{{ $battle->status }} · turn {{ $battle->turn }}</p>@if($battle->status === 'active')<button type="button" wire:click="attack('{{ $battle->id }}')" wire:loading.attr="disabled">Attack</button>@endif</article>@empty<p role="status">No combat is available.</p>@endforelse</section>

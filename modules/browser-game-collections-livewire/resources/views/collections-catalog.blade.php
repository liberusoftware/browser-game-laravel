<section aria-labelledby="browser-game-collections-heading"><h2 id="browser-game-collections-heading">Collections</h2>@if($message)<p role="status">{{ $message }}</p>@endif @forelse($collections as $collection)<article><h3>{{ $collection->name }}</h3><p>{{ $collection->status }}</p><ul>@foreach($collection->entries as $entry)@php($entryProgress = $progress->get($collection->id.':'.$entry->entry_key))<li>{{ $entry->name }} ({{ $entryProgress?->quantity ?? 0 }}/{{ $entry->required_quantity }}) <button type="button" wire:click="record('{{ $collection->id }}', '{{ $entry->entry_key }}')" wire:loading.attr="disabled">Record progress</button></li>@endforeach</ul></article>@empty<p role="status">No collections are available.</p>@endforelse</section>

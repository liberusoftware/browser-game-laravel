<section aria-labelledby="browser-game-live-ops-heading"><h2 id="browser-game-live-ops-heading">Live Ops</h2>@if($message)<p role="status">{{ $message }}</p>@endif @forelse($liveOps as $liveOp)<article><h3>{{ $liveOp->name }}</h3><p>{{ $liveOp->kind }} · {{ $liveOp->status }}</p>@if($liveOp->status === 'published')<button type="button" wire:click="claim('{{ $liveOp->id }}')" wire:loading.attr="disabled">Claim</button>@endif</article>@empty<p role="status">No live ops are available.</p>@endforelse</section>

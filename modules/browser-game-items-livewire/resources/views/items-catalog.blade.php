<section aria-labelledby="browser-game-items-heading"><h2 id="browser-game-items-heading">Items</h2>@if($message)<p role="status">{{ $message }}</p>@endif @forelse($items as $item)<article wire:key="item-{{ $item->getKey() }}"><h3>{{ $item->name }}</h3><p>{{ $item->description }}</p><p>{{ ucfirst($item->rarity) }} {{ ucfirst($item->type) }}</p><button type="button" wire:click="add('{{ $item->getKey() }}')" wire:loading.attr="disabled">Add to inventory</button><button type="button" wire:click="remove('{{ $item->getKey() }}')" wire:loading.attr="disabled">Remove from inventory</button></article>@empty<p role="status">No items are available.</p>@endforelse</section>
